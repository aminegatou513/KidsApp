<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amine.kids_learning">

    <application
        android:allowBackup="true"
        android:icon="@drawable/acc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kids_Learning">
        <activity android:name=".Alphabets"></activity>
        <activity android:name=".alphab"></activity>
        <activity android:name=".Days" />
        <activity android:name=".Numbers" />
        <activity android:name=".animal_desc" />
        <activity android:name=".jungle" />
        <activity android:name=".MainMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>